<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= home.HouseName %> | Home Details</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet" />
  <style>
    /* --- General Body and Font Styling (Dark Theme) --- */
    body {
      font-family: 'Poppins', sans-serif;
      margin: 0;
      padding: 24px 16px;
      background-color: #121821; /* Slightly darker background */
      color: #d1d5db; /* Softer light gray text */
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
      min-height: 100vh;
    }

    /* --- Main Container --- */
    .container {
      max-width: 920px;
      margin: 48px auto;
      background: linear-gradient(145deg, #1f2733, #192028);
      border-radius: 18px;
      overflow: hidden;
      box-shadow:
        0 4px 15px rgba(0, 0, 0, 0.6),
        inset 0 0 40px rgba(255, 255, 255, 0.05);
      border: 1px solid #2a3542;
      transition: box-shadow 0.25s ease;
    }

    .container:hover {
      box-shadow:
        0 8px 30px rgba(0, 0, 0, 0.75),
        inset 0 0 50px rgba(255, 255, 255, 0.1);
    }

    /* --- Home Image Styling --- */
    .home-image {
      width: 100%;
      height: 420px;
      object-fit: cover;
      display: block;
      border-bottom: 3px solid #3bb6ce;
      filter: drop-shadow(0 4px 6px rgba(0, 0, 0, 0.45));
      transition: transform 0.35s ease;
      border-radius: 0 0 18px 18px;
    }

    .container:hover .home-image {
      transform: scale(1.03);
    }

    /* --- Details Section --- */
    .details {
      padding: 42px 48px 48px;
      letter-spacing: 0.015em;
    }

    .details h1 {
      margin-top: 0;
      margin-bottom: 18px;
      font-size: 3rem;
      font-weight: 700;
      line-height: 1.1;
      color: #20c2ff; /* More vibrant blue */
      text-shadow:
        0 0 8px rgba(32, 194, 255, 0.7),
        0 2px 6px rgba(0, 0, 0, 0.4);
    }

    /* --- Information Grid --- */
    .info-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
      gap: 28px;
      margin: 36px 0 12px;
      padding: 20px 26px;
      background-color: #1b222b;
      border-radius: 14px;
      border: 1px solid #2f3a49;
      box-shadow: inset 0 0 10px #0d1722;
      transition: background-color 0.25s ease;
    }

    .info-grid:hover {
      background-color: #202831;
    }

    .info-grid p {
      margin: 0;
      font-size: 1rem;
      color: #a3b1c0;
      user-select: text;
      letter-spacing: 0.02em;
    }

    .info-grid p strong {
      display: block;
      font-weight: 600;
      color: #39c0d0; /* Highlight label */
      margin-bottom: 6px;
      font-size: 0.875rem;
      text-transform: uppercase;
      letter-spacing: 0.12em;
    }

    /* --- Description Styling --- */
    .description {
      margin-top: 36px;
      line-height: 1.9;
      font-size: 1rem;
      color: #cfd8e4;
      user-select: text;
      font-weight: 400;
    }

    .description p {
      margin-bottom: 18px;
      text-align: justify;
    }

    /* --- Buttons Section --- */
    .btns {
      margin-top: 48px;
      display: flex;
      align-items: center;
      justify-content: flex-start;
      flex-wrap: wrap;
      gap: 18px;
    }

    .btns a,
    .btns button {
      padding: 14px 30px;
      font-size: 1.1rem;
      font-weight: 600;
      border-radius: 10px;
      cursor: pointer;
      text-decoration: none;
      user-select: none;
      border: 2.5px solid transparent;
      text-align: center;
      transition:
        background-color 0.35s ease,
        color 0.35s ease,
        box-shadow 0.35s ease,
        transform 0.2s ease;
      min-width: 150px;
      display: inline-flex;
      justify-content: center;
      align-items: center;
      letter-spacing: 0.04em;
    }

    /* Back Link — secondary */
    .btns a {
      background-color: transparent;
      color: #20c2ff;
      border-color: #20c2ff;
      box-shadow: 0 0 15px rgba(32, 194, 255, 0.5);
    }

    .btns a:hover,
    .btns a:focus-visible {
      background-color: #20c2ff;
      color: #121821;
      border-color: #17a9bf;
      box-shadow:
        0 5px 15px rgba(32, 194, 255, 0.8),
        inset 0 0 12px #17a9bf;
      outline: none;
      transform: scale(1.04);
    }

    /* Primary Book Button */
    .btns button {
      background-color: #17a9bf;
      color: #eaf6f8;
      border-color: #17a9bf;
      box-shadow: 0 8px 20px rgba(23, 169, 191, 0.45);
      font-weight: 700;
      letter-spacing: 0.05em;
    }

    .btns button:hover,
    .btns button:focus-visible {
      background-color: #24c7d1;
      border-color: #24c7d1;
      box-shadow:
        0 10px 30px rgba(36, 199, 209, 0.7),
        inset 0 0 15px #24c7d1;
      outline: none;
      transform: translateY(-3px) scale(1.07);
    }

    .btns button:active {
      transform: translateY(0) scale(1);
      box-shadow: 0 6px 15px rgba(36, 199, 209, 0.6);
    }

    /* Responsive Adjustments */
    @media (max-width: 600px) {
      body {
        padding: 20px 12px;
      }

      .container {
        margin: 24px auto;
        border-radius: 14px;
      }

      .home-image {
        height: 280px;
        border-radius: 0 0 14px 14px;
      }

      .details {
        padding: 28px 22px 36px;
      }

      .details h1 {
        font-size: 2.4rem;
        margin-bottom: 14px;
      }

      .info-grid {
        gap: 18px;
        padding: 18px 16px;
        grid-template-columns: 1fr 1fr;
      }

      .btns {
        flex-direction: column;
        gap: 14px;
      }

      .btns a,
      .btns button {
        min-width: 100%;
        font-size: 1rem;
        padding: 14px 0;
      }
    }
  </style>
</head>

<body>

  <div class="container" role="main" aria-label="Home Details for <%= home.HouseName %>">
    <img src="<%= home.PhotoUrl %>" alt="Image of <%= home.HouseName %>" class="home-image" />

    <div class="details">
      <h1><%= home.HouseName %></h1>

      <div class="info-grid" aria-label="Home basic information">
        <p><strong>Owned by:</strong> <%= home.OwnerName %></p>
        <p><strong>Location:</strong> <%= home.Location %></p>
        <p><strong>Price per night:</strong> ₹<%= home.Rate %></p>
        <p><strong>Rating:</strong> ⭐ <%= home.Rating %> / 5</p>
      </div>

      <section class="description" aria-label="Property description">
        <p>Welcome to <strong><%= home.HouseName %></strong>, your perfect getaway destination! Nestled in the heart of <%= home.Location %>, this stunning property combines modern design with a cozy ambiance to make you feel right at home.</p>
        <p>Enjoy peaceful mornings on the private balcony, relax in spacious and beautifully decorated rooms, and take advantage of all the premium amenities including high-speed Wi-Fi, a fully equipped kitchen, and 24/7 support. Whether you’re traveling solo, as a couple, or with family, this space is tailored for comfort and convenience.</p>
        <p>Book now to experience unforgettable stays, top-rated service, and a home you'll want to return to again and again.</p>
      </section>

      <nav class="btns" aria-label="Navigation and booking options">
        <a href="/" role="link" tabindex="0">← Back to Listings</a>
        <button type="button" tabindex="0">Book Now</button>
      </nav>
    </div>
  </div>

  <%- include('partials/footer') %>
</body>

</html>
