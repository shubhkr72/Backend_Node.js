<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Homes Listing - My Rental App</title>
  <style>
    body {
      background-color: #1e1e2f;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      margin: 0;
      padding: 0;
      color: #e0e0e0;
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
    }

    h1 {
      text-align: center;
      color: #4db8ff;
      margin-top: 40px;
      font-weight: 700;
      font-size: 2.5rem;
      letter-spacing: 0.05em;
      text-shadow: 0 0 6px rgba(77, 184, 255, 0.6);
    }

    #link {
      text-align: center;
      margin-top: 20px;
    }

    #link a {
      color: #4db8ff;
      text-decoration: none;
      font-weight: 600;
      padding: 12px 26px;
      background-color: #2c2c3c;
      border-radius: 12px;
      box-shadow: 0 6px 12px rgba(77, 184, 255, 0.45);
      transition: background-color 0.3s ease, box-shadow 0.3s ease;
      display: inline-block;
      font-size: 1rem;
    }

    #link a:hover {
      background-color: #3d4a6a;
      box-shadow: 0 10px 20px rgba(77, 184, 255, 0.7);
    }

    /* Container for all cards */
    .card-grid {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 28px;
      padding: 40px 20px 60px 20px;
      max-width: 1280px;
      margin: 0 auto;
    }

    .home-card {
      background-color: #2c2c3c;
      border-radius: 16px;
      width: 300px;
      overflow: hidden;
      box-shadow:
        0 10px 15px rgba(0, 0, 0, 0.25),
        0 4px 6px rgba(77, 184, 255, 0.15);
      color: #e0e0e0;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      display: flex;
      flex-direction: column;
      cursor: pointer;
    }

    .home-card:hover {
      transform: translateY(-8px) scale(1.05);
      box-shadow:
        0 20px 30px rgba(0, 0, 0, 0.3),
        0 8px 12px rgba(77, 184, 255, 0.4);
    }

    .home-photo {
      width: 100%;
      height: 210px;
      object-fit: cover;
      background-color: #1f1f2f;
      border-bottom: 3px solid #4db8ff;
      transition: transform 0.3s ease;
    }

    .home-card:hover .home-photo {
      transform: scale(1.05);
    }

    .home-info {
      padding: 20px 24px 28px 24px;
      flex-grow: 1;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
    }

    .home-title {
      margin: 0 0 14px 0;
      font-size: 1.5rem;
      color: #4db8ff;
      font-weight: 700;
      text-shadow: 0 0 8px rgba(77, 184, 255, 0.6);
    }

    .home-info p {
      margin: 6px 0;
      font-size: 0.95rem;
      line-height: 1.3;
      color: #cfcfcf;
    }

    .home-info p strong {
      color: #a0c4ff;
      font-weight: 600;
    }

    /* Button container for spacing */
    .btn-group {
      display: flex;
      justify-content: center;
      gap: 16px;
      margin-top: 18px;
    }

    .rent-btn {
      background-color: #4db8ff;
      color: #1e1e2f;
      padding: 12px 26px;
      border-radius: 30px;
      font-weight: 700;
      font-size: 1rem;
      border: none;
      cursor: pointer;
      text-decoration: none;
      box-shadow: 0 6px 12px rgba(77, 184, 255, 0.5);
      transition: background-color 0.3s ease, box-shadow 0.3s ease, transform 0.2s ease;
      user-select: none;
      display: inline-block;
      text-align: center;
    }

    .rent-btn:hover,
    .rent-btn:focus {
      background-color: #3a99e0;
      box-shadow: 0 10px 20px rgba(77, 184, 255, 0.7);
      transform: translateY(-2px);
      color: #fff;
      outline: none;
    }

    /* Secondary button style */
    .rent-btn.secondary {
      background-color: #2c2c3c;
      color: #4db8ff;
      box-shadow: none;
      border: 2px solid #4db8ff;
    }

    .rent-btn.secondary:hover,
    .rent-btn.secondary:focus {
      background-color: #4db8ff;
      color: #1e1e2f;
      box-shadow: 0 6px 12px rgba(77, 184, 255, 0.5);
      transform: translateY(-2px);
      outline: none;
    }

    @media (max-width: 900px) {
      .home-card {
        width: 45%;
      }
    }

    @media (max-width: 600px) {
      .home-card {
        width: 90%;
      }
    }
  </style>
</head>
<body>
  <%- include('partials/nav') %>

  <div>
    <!-- Optional welcome or header -->
  </div>

  <div>
    <h1>Here Are Listed Homes</h1>
    <div class="card-grid">
      <% RegisteredHomes.forEach((home) => { %>
        <div class="home-card">
          <img class="home-photo" src="<%= home.PhotoUrl %>" alt="Image of <%= home.HouseName %>" />
          <div class="home-info">
            <h2 class="home-title"><%= home.HouseName %></h2>
            <p><strong>Owned by:</strong> <%= home.OwnerName %></p>
            <p><strong>Location:</strong> <%= home.Location %></p>
            <p><strong>Price per night:</strong> ₹<%= home.Rate %></p>
            <p><strong>Rating:</strong> ⭐ <%= home.Rating %></p>
            <div class="btn-group">
              <a href="/home/<%= home._id %>" class="rent-btn secondary" tabindex="0">Details</a>
              <a href="/home/<%= home._id %>/book" class="rent-btn" tabindex="0">Book</a>
            </div>
          </div>
        </div>
      <% }) %>
    </div>
  </div>

  <%- include('partials/footer') %>

</body>
</html>
